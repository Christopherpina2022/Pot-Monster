<UserControl x:Class="FGC_Stat_Analyzer_wpf.Views.UserControls.Sidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FGC_Stat_Analyzer_wpf.Views.UserControls"
             xmlns:UserControls="clr-namespace:FGC_Stat_Analyzer_wpf.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="40*"/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="optionCombo" Text="Placeholder" Margin="5" VerticalAlignment="Center" ItemsSource="{Binding optionCombo}" SelectedIndex="0" SelectionChanged="optionCombo_SelectionChanged" Focusable="False"/>
        <UserControls:TextInput Grid.Row="1" Margin="5" Placeholder="Tournament Name"/>
        <UserControls:TextInput Grid.Row="2" Margin="5" Placeholder="State Code"/>
        <UserControls:TextInput x:Name="perPage" Grid.Row="3" Margin="5" Placeholder="Per Page (Optional)" FontStyle="Italic" PlaceholderColor="LightYellow"/>
        <UserControls:TextInput x:Name="startDate" Grid.Row="4" Margin="5" Placeholder="Start Date (Optional)" FontStyle="Italic" PlaceholderColor="LightYellow"/>
        <UserControls:TextInput x:Name="endDate" Grid.Row="5" Margin="5" Placeholder="End Date (Optional)" FontStyle="Italic" PlaceholderColor="LightYellow"/>
        <CheckBox x:Name="btnYTD" Grid.Row= "6" FontSize="20" Content="Year To Date" VerticalContentAlignment="Center" HorizontalAlignment="Center" Checked="btnYTD_Checked" Unchecked="btnYTD_Unchecked" Focusable="False"/>
    </Grid>
</UserControl>
